---
phase: 7
plan: 07-03
title: "Surfaces Feedback And Foundation Audit"
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - components/ui/card.tsx
  - components/ui/badge.tsx
  - components/ui/dialog.tsx
  - components/ui/alert-dialog.tsx
  - components/ui/sonner.tsx
  - components/ui/tag-input.tsx
  - components/ui/separator.tsx
autonomous: true
requirements: [UI-08]
---

# Plan 07-03: Surfaces Feedback And Foundation Audit

<objective>
Update shared surface, feedback, overlay, and tag primitives to the light dashboard contract, then run a foundation audit that proves the default shared UI no longer depends on dark theme remnants, oversized radii, or serif primitive headings.
</objective>

<requirements_addressed>
UI-08
</requirements_addressed>

<truths>
- Preserve exports and props for all primitives.
- Do not add new product content, charts, metrics, routes, or analytics.
- Product page restyling belongs to Phase 8; this plan updates shared primitives and audits foundation files only.
- Any remaining dark token references must be limited to an intentionally retained `.dark` fallback and justified in the summary.
</truths>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-07-07: Overlay/toast changes reduce readability or keyboard interaction | MEDIUM | Preserve Radix/Sonner structure, focus states, props passthrough, and run lint/build. |
| T-07-08: Dark hard-coded values remain in shared primitives | MEDIUM | Run dark-token grep against `app/globals.css`, `components/ui`, and `app/layout.tsx`; fix unapproved matches. |
| T-07-09: Radius/typography violations preserve the previous design direction | LOW | Run radius/font grep and remove `font-heading`, `rounded-2xl`, `rounded-[14px]`, and `rounded-[20px]` from shared primitives. |
</threat_model>

<tasks>
  <task id="07-03-01" type="execute">
    <title>Convert card, badge, dialog, alert dialog, toast, tag input, and separator primitives</title>
    <read_first>
      - `.planning/phases/07-light-visual-foundation/07-UI-SPEC.md`
      - `.planning/phases/07-light-visual-foundation/07-RESEARCH.md`
      - `components/ui/card.tsx`
      - `components/ui/badge.tsx`
      - `components/ui/dialog.tsx`
      - `components/ui/alert-dialog.tsx`
      - `components/ui/sonner.tsx`
      - `components/ui/tag-input.tsx`
      - `components/ui/separator.tsx`
    </read_first>
    <files>
      - `components/ui/card.tsx`
      - `components/ui/badge.tsx`
      - `components/ui/dialog.tsx`
      - `components/ui/alert-dialog.tsx`
      - `components/ui/sonner.tsx`
      - `components/ui/tag-input.tsx`
      - `components/ui/separator.tsx`
    </files>
    <action>
      Update surface and feedback primitives to these exact contracts:
      - `Card`: use `rounded-lg`, `border-border`, `bg-card`, `shadow-[0_12px_30px_rgba(35,45,72,0.05)]`, `gap-4`, `py-6`, and remove `rounded-2xl`.
      - `CardHeader`: use `gap-1.5`, `px-6`, no rounded header class.
      - `CardTitle`: remove `font-heading`; use `text-[15px] leading-snug font-medium tracking-normal`.
      - `CardFooter`: use `border-t border-border bg-secondary/70 p-6`, no rounded footer class.
      - `Badge`: keep variants but set light values: default `border-border bg-muted text-muted-foreground`; secondary `border-[#d8f4f1] bg-[#e7faf8] text-[#249c96]`; destructive `border-destructive/20 bg-[#fdecef] text-destructive`; outline `border-border bg-card text-muted-foreground`.
      - `DialogOverlay` and `AlertDialogOverlay`: `bg-[rgba(15,23,42,0.22)]`.
      - `DialogContent` and `AlertDialogContent`: `rounded-xl`, `border-border`, `bg-popover`, `shadow-[0_24px_70px_rgba(35,45,72,0.18)]`.
      - `DialogTitle` and `AlertDialogTitle`: remove `font-heading`; use `text-xl leading-tight font-semibold tracking-normal`.
      - `Toaster`: set `--normal-bg: #ffffff`, `--normal-text: #4b5363`, `--normal-border: #e7eaf0`, `--border-radius: 8px`, and toast shadow `shadow-[0_12px_30px_rgba(35,45,72,0.10)]`.
      - `TagInput`: use `min-h-10`, `rounded-lg`, `border-input`, `bg-card`, muted placeholder token, and preserve hidden JSON input plus add/remove keyboard behavior.
      - `Separator`: keep token-driven `bg-border`.
    </action>
    <verify>
      - `rg 'rounded-lg|shadow-\\[0_12px_30px_rgba\\(35,45,72,0.05\\)\\]|text-\\[15px\\]' components/ui/card.tsx`
      - `rg '#e7faf8|#249c96|#fdecef' components/ui/badge.tsx`
      - `rg 'rgba\\(15,23,42,0.22\\)|rounded-xl|shadow-\\[0_24px_70px_rgba\\(35,45,72,0.18\\)\\]|text-xl' components/ui/dialog.tsx components/ui/alert-dialog.tsx`
      - `rg '#ffffff|#4b5363|#e7eaf0|--border-radius.: \"8px\"' components/ui/sonner.tsx`
      - `rg 'rounded-lg|bg-card|placeholder:text-muted-foreground|min-h-10' components/ui/tag-input.tsx`
      - `npm run lint`
    </verify>
    <acceptance_criteria>
      - `components/ui/card.tsx` contains `shadow-[0_12px_30px_rgba(35,45,72,0.05)]`.
      - `components/ui/card.tsx` does not contain `font-heading`.
      - `components/ui/badge.tsx` contains `#e7faf8`.
      - `components/ui/dialog.tsx` contains `bg-[rgba(15,23,42,0.22)]`.
      - `components/ui/alert-dialog.tsx` contains `rounded-xl`.
      - `components/ui/sonner.tsx` contains `"--normal-bg": "#ffffff"`.
      - `components/ui/tag-input.tsx` contains `placeholder:text-muted-foreground`.
      - `npm run lint` exits 0.
    </acceptance_criteria>
  </task>

  <task id="07-03-02" type="execute">
    <title>Run foundation audit and remove shared primitive violations</title>
    <read_first>
      - `.planning/phases/07-light-visual-foundation/07-UI-SPEC.md`
      - `.planning/phases/07-light-visual-foundation/07-VALIDATION.md`
      - `app/globals.css`
      - `app/layout.tsx`
      - `components/ui/button.tsx`
      - `components/ui/card.tsx`
      - `components/ui/input.tsx`
      - `components/ui/textarea.tsx`
      - `components/ui/select.tsx`
      - `components/ui/checkbox.tsx`
      - `components/ui/badge.tsx`
      - `components/ui/dialog.tsx`
      - `components/ui/alert-dialog.tsx`
      - `components/ui/sonner.tsx`
      - `components/ui/tag-input.tsx`
      - `components/ui/separator.tsx`
      - `components/ui/label.tsx`
    </read_first>
    <files>
      - `app/globals.css`
      - `app/layout.tsx`
      - `components/ui/button.tsx`
      - `components/ui/card.tsx`
      - `components/ui/input.tsx`
      - `components/ui/textarea.tsx`
      - `components/ui/select.tsx`
      - `components/ui/checkbox.tsx`
      - `components/ui/badge.tsx`
      - `components/ui/dialog.tsx`
      - `components/ui/alert-dialog.tsx`
      - `components/ui/sonner.tsx`
      - `components/ui/tag-input.tsx`
      - `components/ui/separator.tsx`
      - `components/ui/label.tsx`
    </files>
    <action>
      Run the validation grep commands from `07-VALIDATION.md`. For any match in shared foundation files, remove or replace the violating class/value unless it is an intentional `.dark` fallback in `app/globals.css`.

      Required cleanup targets:
      - Remove default-root dark class matches.
      - Remove hard-coded default dark foundation hex values from `components/ui/*`, `app/layout.tsx`, and non-`.dark` portions of `app/globals.css`: `#090d17`, `#111827`, `#0d1320`, `#171c2a`.
      - Remove `font-heading` from `components/ui/*` and the global heading base selector.
      - Remove `rounded-2xl`, `rounded-3xl`, `rounded-4xl`, `rounded-[14px]`, and `rounded-[20px]` from `components/ui/*`.
      - Preserve `focus-visible`, `disabled:`, `aria-invalid`, and relevant Radix state classes.

      Finish by running `npm run lint` and `npm run build`. If page-level files outside foundation still have old dark classes, do not refactor them in Phase 7; record them in the executor summary for Phase 8.
    </action>
    <verify>
      - `npm run lint`
      - `npm run build`
      - `rg 'className=\\{cn\\(\"dark\"|<html[^>]*dark' app components || true`
      - `rg '#090d17|#111827|#0d1320|#171c2a' components/ui app/layout.tsx || true`
      - `rg 'font-heading|rounded-2xl|rounded-3xl|rounded-4xl|rounded-\\[14px\\]|rounded-\\[20px\\]' components/ui app/layout.tsx || true`
      - `rg 'focus-visible|disabled:|aria-invalid|aria-expanded|data-\\[state=' components/ui`
    </verify>
    <acceptance_criteria>
      - `npm run lint` exits 0.
      - `npm run build` exits 0.
      - `rg 'className=\\{cn\\(\"dark\"|<html[^>]*dark' app components` returns no matches.
      - `rg '#090d17|#111827|#0d1320|#171c2a' components/ui app/layout.tsx` returns no matches.
      - `rg 'font-heading|rounded-2xl|rounded-3xl|rounded-4xl|rounded-\\[14px\\]|rounded-\\[20px\\]' components/ui app/layout.tsx` returns no matches.
      - `rg 'focus-visible|disabled:|aria-invalid|aria-expanded|data-\\[state=' components/ui` returns matches.
    </acceptance_criteria>
  </task>
</tasks>

<verification>
Run:
- `npm run lint`
- `npm run build`
- `rg 'className=\\{cn\\(\"dark\"|<html[^>]*dark' app components || true`
- `rg '#090d17|#111827|#0d1320|#171c2a' components/ui app/layout.tsx || true`
- `rg 'font-heading|rounded-2xl|rounded-3xl|rounded-4xl|rounded-\\[14px\\]|rounded-\\[20px\\]' components/ui app/layout.tsx || true`
</verification>

<success_criteria>
- Shared surface and overlay primitives match the light UI-SPEC.
- Foundation files no longer depend on default dark hard-coded values.
- Shared primitives no longer use old oversized radii or serif heading classes.
- Build and lint pass.
</success_criteria>

<must_haves>
- UI-08 is satisfied by the final foundation audit, build, lint, and preserved state classes.
</must_haves>
