---
phase: 8
plan: 08-04
title: Buyers And Profile Surfaces
type: execute
wave: 3
depends_on:
  - 08-01
files_modified:
  - app/(app)/buyers/page.tsx
  - components/buyers/buyers-table.tsx
  - app/(app)/profile/page.tsx
autonomous: true
requirements:
  - APP-04
  - DEAL-02
  - DEAL-03
---

<objective>
Refactor buyers and profile authenticated surfaces into the light workspace style while preserving current buyer query, table actions, and account detail display.
</objective>

<threat_model>
Threats:
- MEDIUM: Buyers query changes could expose another user's buyers.
- MEDIUM: Removing buyer action triggers could break edit/delete workflows.
- LOW: Profile page could display less account context.

Mitigations:
- Preserve `.eq('user_id', user.id)` in `app/(app)/buyers/page.tsx`.
- Preserve `BuyerFormModal` and `DeleteBuyerDialog` usage in `components/buyers/buyers-table.tsx`.
- Preserve `user?.email` and `user?.id` in profile display.
</threat_model>

<tasks>
  <task id="08-04-T1" type="execute">
    <title>Refactor buyers page shell</title>
    <read_first>
      - app/(app)/buyers/page.tsx
      - components/buyers/buyers-table.tsx
      - .planning/phases/08-authenticated-workspace-refactor/08-UI-SPEC.md
    </read_first>
    <files>
      - app/(app)/buyers/page.tsx
    </files>
    <action>
      Keep the Supabase buyer query unchanged. Remove the outer `p-8` wrapper and use `space-y-6`. Replace `font-heading` with `text-2xl font-semibold leading-tight text-foreground`. Keep helper copy `Manage your buyer contacts and tags.`. Keep `<BuyerFormModal />` visible when buyers exist. Use responsive header classes `flex flex-col gap-4 md:flex-row md:items-center md:justify-between`.
    </action>
    <acceptance_criteria>
      - `app/(app)/buyers/page.tsx` still contains `.eq('user_id', user.id)`.
      - `app/(app)/buyers/page.tsx` still contains `Manage your buyer contacts and tags.`
      - `app/(app)/buyers/page.tsx` still contains `<BuyerFormModal />`.
      - `app/(app)/buyers/page.tsx` contains `space-y-6`.
      - `rg "font-heading|className=\"p-8\"" app/'(app)'/buyers/page.tsx` returns no matches.
    </acceptance_criteria>
  </task>

  <task id="08-04-T2" type="execute">
    <title>Restyle buyers table and empty state</title>
    <read_first>
      - components/buyers/buyers-table.tsx
      - components/buyers/buyer-form-modal.tsx
      - components/buyers/delete-buyer-dialog.tsx
      - .planning/phases/08-authenticated-workspace-refactor/08-UI-SPEC.md
    </read_first>
    <files>
      - components/buyers/buyers-table.tsx
    </files>
    <action>
      Replace the empty state with a white panel using `rounded-lg border border-border bg-card p-8 text-center shadow-[0_12px_30px_rgba(35,45,72,0.05)]`, remove `font-heading`, and keep copy `No buyers yet.` and `Add your first buyer to get started.`. Restyle the table container with `rounded-lg border border-border bg-card shadow-[0_12px_30px_rgba(35,45,72,0.05)]`. Use header fill `bg-[#fbfcfe]`. Use rows with `min-h-12`, `hover:bg-[#f8fafc]`, and tag pills `bg-[#f3f5f9] text-[#6b7280] border-[#e7eaf0]`. Preserve `BuyerFormModal` edit trigger and `DeleteBuyerDialog`.
    </action>
    <acceptance_criteria>
      - `components/buyers/buyers-table.tsx` still contains `No buyers yet.`
      - `components/buyers/buyers-table.tsx` still contains `Add your first buyer to get started.`
      - `components/buyers/buyers-table.tsx` still contains `BuyerFormModal`.
      - `components/buyers/buyers-table.tsx` still contains `DeleteBuyerDialog`.
      - `components/buyers/buyers-table.tsx` contains `bg-[#fbfcfe]`.
      - `components/buyers/buyers-table.tsx` contains `bg-[#f3f5f9] text-[#6b7280] border-[#e7eaf0]`.
      - `rg "font-heading|rounded-xl|text-zinc" components/buyers/buyers-table.tsx` returns no matches.
    </acceptance_criteria>
  </task>

  <task id="08-04-T3" type="execute">
    <title>Restyle profile page</title>
    <read_first>
      - app/(app)/profile/page.tsx
      - .planning/phases/08-authenticated-workspace-refactor/08-UI-SPEC.md
    </read_first>
    <files>
      - app/(app)/profile/page.tsx
    </files>
    <action>
      Remove outer `p-8` and use `space-y-6`. Replace `font-heading` with `text-2xl font-semibold`. Restyle the account info card as `max-w-xl rounded-lg border border-border bg-card p-6 shadow-[0_12px_30px_rgba(35,45,72,0.05)]`. Preserve displayed email and user id values. Keep labels as uppercase muted metadata.
    </action>
    <acceptance_criteria>
      - `app/(app)/profile/page.tsx` still contains `user?.email`.
      - `app/(app)/profile/page.tsx` still contains `user?.id`.
      - `app/(app)/profile/page.tsx` contains `max-w-xl rounded-lg border border-border bg-card p-6`.
      - `rg "font-heading|className=\"p-8\"" app/'(app)'/profile/page.tsx` returns no matches.
    </acceptance_criteria>
  </task>
</tasks>

<verification>
Run:

```bash
npm run lint
rg "font-heading|className=\"p-8\"|text-zinc|rounded-xl" app/'(app)'/buyers/page.tsx components/buyers/buyers-table.tsx app/'(app)'/profile/page.tsx
rg "\\.eq\\('user_id', user.id\\)|BuyerFormModal|DeleteBuyerDialog|user\\?\\.email|user\\?\\.id" app/'(app)'/buyers/page.tsx components/buyers/buyers-table.tsx app/'(app)'/profile/page.tsx
```

Expected: lint passes; forbidden grep returns no matches; required behavior strings are present.
</verification>

<success_criteria>
- Buyers and profile surfaces align with the light workspace.
- Buyer query and edit/delete actions are preserved.
- No nested app page padding remains in buyers/profile pages.
</success_criteria>

<must_haves>
- Preserve buyer ownership filtering.
- Preserve buyer action triggers.
- Preserve profile email and user id display.
</must_haves>
